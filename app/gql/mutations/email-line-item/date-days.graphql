mutation EmailLineItemDateDays($input: EmailLineItemDateDaysMutationInput!) {
  emailLineItemDateDays(input: $input) {
    id
    status
    requires
    dates {
      type
      start
      end
      days
    }
  }
}
